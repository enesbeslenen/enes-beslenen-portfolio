/**
 * Sanity CMS client — placeholder.
 *
 * When ready to connect:
 *   1. npm install next-sanity
 *   2. Fill in projectId, dataset, and apiVersion below.
 *   3. Remove the placeholder export and uncomment the real client.
 */

// import { createClient } from "next-sanity";
//
// export const sanityClient = createClient({
//   projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID!,
//   dataset:   process.env.NEXT_PUBLIC_SANITY_DATASET ?? "production",
//   apiVersion: "2024-01-01",
//   useCdn: process.env.NODE_ENV === "production",
// });

export const sanityClient = null; // replace when CMS is wired up
